<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} — Вячеслав Добранов</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  {% include "header.njk" %}
  <main class="{{ main_class | default('columns') }}">
    {{ content | safe }}
  </main>
  {% include "footer.njk" %}
</body>
</html>